# .github/workflows/main.yml
name: Stem Extractor CI

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  # ==========================================
  # JOB 1: PYTHON ML LOGIC TESTS
  # ==========================================
  test-python:
    name: Python Unit Tests
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest torch torchaudio soundfile

    - name: Run PyTest
      run: pytest tests/Python/

  # ==========================================
  # JOB 2: C++ FIFO & BUFFER TESTS
  # ==========================================
  test-cpp:
    name: C++ Unit Tests
    runs-on: macos-latest
    
    steps:
    - name: Checkout Code with Submodules
      uses: actions/checkout@v4
      with:
        submodules: recursive 

    - name: Bypass CMake ONNX Requirement
      run: |
        # Creates a 0-byte file so juce_add_binary_data doesn't crash
        touch plugin/stem_extractor.onnx

    - name: Configure CMake
      run: cmake -B build -S plugin -DCMAKE_BUILD_TYPE=Release

    - name: Compile Tests Only
      # By specifying the target, we skip compiling the entire VST3 plugin!
      run: cmake --build build --target RunCPPTests --parallel 4

    - name: Run CTest
      run: ctest --test-dir build --output-on-failure

